#include <bits/stdc++.h>
using namespace std;
using ll = long long;
typedef pair<ll, ll> P;
#define rep(i, s, n) for (ll i = s; i < (ll)(n); i++)

ll solve(ll n, ll l, ll r) {
  ll ans = 0;

  return ans;
}

int main() {
  ll N;
  cin >> N;
  rep(i, 0, N) {
    ll le, l, r;
    cin >> le >> l >> r;
    cout << solve(le, l, r) << endl;
  }
}